{
  "buildCommand": "vite build && esbuild api/boot.ts --platform=node --bundle --format=esm --outdir=dist --banner:js=\"import { createRequire } from 'module';const require = createRequire(import.meta.url);\"",
  "outputDirectory": "dist/public",
  "functions": {
    "api/boot.ts": {
      "runtime": "@vercel/node@5"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/boot.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
